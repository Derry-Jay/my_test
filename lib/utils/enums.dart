enum CardType { filled, outlined }

enum PickType { image, media, video }

enum ButtonType { raised, text, border }

enum APIMode { dev, test, prod, staging }

enum MeasurementUnitGroup { metric, imperial }

enum TrainBerth { lower, upper, middle, sideLower, sideUpper }

enum TransitionType { normal, fade, scale, slide, size, rotate }

enum HTTPMethodType { delete, download, fetch, head, patch, post, put }

enum LoaderType {
  chasingDots,
  circle,
  cubeGrid,
  dancingSquare,
  doubleBounce,
  dualRing,
  fadingCircle,
  fadingCube,
  fadingFour,
  fadingGrid,
  foldingCube,
  hourGlass,
  normal,
  pianoWave,
  pouringHourGlass,
  pouringHourGlassRefined,
  pulse,
  pulsingGrid,
  pumpingHeart,
  ring,
  ripple,
  threeInOut,
  threeBounce,
  squareCircle,
  spinningLines,
  rotatingPlain,
  rotatingCircle,
  spinningCircle,
  wanderingCubes,
  wave,
  waveSpinner
}
